export interface ScrcpyOptions {
  turn_screen_off: boolean;
  stay_awake: boolean;
  show_touches: boolean;
  max_fps?: number;
  bit_rate_mb?: number;
  record_to_file: boolean;
  always_on_top: boolean;
  no_audio: boolean;
  read_only: boolean;
}

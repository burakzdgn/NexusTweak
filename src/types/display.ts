export interface ResolutionPreset {
  label: string;
  width: number;
  height: number;
  description: string;
}

export interface DensityPreset {
  label: string;
  density: number;
  description: string;
}
